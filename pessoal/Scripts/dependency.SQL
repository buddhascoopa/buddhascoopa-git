SELECT DISTINCT A.OWNER || '.' || A.TABLE_NAME
FROM SYS.DBA_CONSTRAINTS B,SYS.DBA_CONSTRAINTS A
WHERE B.TABLE_NAME='EXT_USR_UF'
AND B.OWNER='USR_WEB'
AND B.CONSTRAINT_TYPE IN ('P', 'U')
AND A.CONSTRAINT_TYPE='R'
AND B.OWNER=A.R_OWNER
AND B.CONSTRAINT_NAME=A.R_CONSTRAINT_NAME
ORDER BY 1
